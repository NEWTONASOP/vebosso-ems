// ============================================================================
// VEBOSSO EMS — Member Venues
// ============================================================================

import { VenuesScreen } from '../../components/VenuesScreen';

export default function MemberVenuesScreen() {
  return <VenuesScreen canManage={false} showBack />;
}
